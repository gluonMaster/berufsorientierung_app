# 🎯 КОНТЕКСТ ПРОЕКТА: Berufsorientierung - Система регистрации на мероприятия

## 📌 ТЕКУЩЕЕ СОСТОЯНИЕ

Проект **полностью реализован** и **задеплоен** на Cloudflare Workers согласно инструкции `docs/development/DEPLOYMENT.md` (Шаги 1-7 выполнены).

**Статус:** Post-deployment debugging & refinement  
**Дата деплоя:** 13 ноября 2025  
**Production URL:** https://berufsorientierung-app.berufsevents25.workers.dev

### ✅ Что работает:

- **Cloudflare D1 Database** настроена, миграции применены
- **Cloudflare R2 Storage** для QR-кодов настроен
- **Cloudflare Workers** деплой успешен
- **Cloudflare Turnstile** интегрирован (защита от ботов)
- **Регистрация пользователей** работает
- **Авторизация** работает (JWT в httpOnly cookies)
- **Админ-панель** доступна, но имеет проблемы с интерфейсом и не функционирует как задумано
- **База данных** содержит тестовых пользователей и админов
- **DNS записи** (SPF, DKIM, DMARC) настроены

### ⚠️ Выявленные проблемы:

После деплоя обнаружены множественные проблемы:

1. **Email отправка**: MailChannels возвращает 401 Unauthorized (скорее всего проблемы связаны с тем, что почтовый домен kolibri-dresden.de не находится в зоне CloudFlare)
2. **Turnstile**: Токены истекают, нет автоматического сброса виджета после ошибок
3. **Интерфейс**: Возможны проблемы с UX/UI, мобильной адаптацией
4. **Логика**: Возможны баги в бизнес-логике (запись на мероприятия, отмена, GDPR)
5. **i18n**: Проблемы с SSR рендерингом, locale не инициализируется на сервере

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА

### Технологический стек:

**Frontend:**

- SvelteKit 2.x (TypeScript, Svelte 5 runes)
- Tailwind CSS
- svelte-i18n (мультиязычность: de/en/ru/uk)

**Backend:**

- Cloudflare Workers (serverless)
- Cloudflare D1 (SQLite database)
- Cloudflare R2 (file storage для QR-кодов)

**Библиотеки:**

- Zod (валидация)
- bcryptjs (хеширование паролей)
- jose (JWT через WebCrypto)
- qrcode (генерация QR-кодов)
- MailChannels (email через HTTP API)

---

## 📁 СТРУКТУРА ПРОЕКТА

```
berufsorientierung_app/
├── src/
│   ├── routes/                          # SvelteKit routes
│   │   ├── +layout.svelte               # Root layout (Header, Footer, i18n init)
│   │   ├── +layout.server.ts            # Server layout (user auth, locale detection)
│   │   ├── +page.svelte                 # Главная страница (список мероприятий)
│   │   ├── register/+page.svelte        # Регистрация пользователя
│   │   ├── login/+page.svelte           # Вход
│   │   ├── profile/+page.svelte         # Личный кабинет
│   │   ├── events/[id]/register/        # Запись на мероприятие
│   │   ├── admin/                       # Админ-панель
│   │   │   ├── +layout.server.ts        # Middleware проверки прав админа
│   │   │   ├── +page.svelte             # Dashboard
│   │   │   ├── events/+page.svelte      # Управление мероприятиями
│   │   │   ├── users/+page.svelte       # Управление пользователями
│   │   │   ├── registrations/+page.svelte # Управление записями
│   │   │   ├── newsletter/+page.svelte  # Массовая рассылка
│   │   │   ├── stats/+page.svelte       # Статистика
│   │   │   └── logs/+page.svelte        # Логи действий
│   │   └── api/                         # API endpoints
│   │       ├── auth/                    # Аутентификация
│   │       │   ├── register/+server.ts  # POST регистрация
│   │       │   ├── login/+server.ts     # POST вход
│   │       │   └── logout/+server.ts    # POST выход
│   │       ├── profile/
│   │       │   ├── update/+server.ts    # PUT обновление профиля
│   │       │   └── delete/+server.ts    # POST удаление (GDPR)
│   │       ├── events/
│   │       │   ├── register/+server.ts  # POST запись на событие
│   │       │   └── cancel/+server.ts    # POST отмена записи
│   │       ├── admin/                   # Админские API
│   │       │   ├── events/              # CRUD мероприятий
│   │       │   ├── users/               # Управление пользователями
│   │       │   ├── registrations/       # Управление записями
│   │       │   └── newsletter/          # Массовая рассылка
│   │       └── cron/
│   │           └── delete-users/+server.ts # Cron для автоудаления
│   │
│   ├── lib/
│   │   ├── components/                  # Svelte компоненты
│   │   │   ├── ui/                      # Базовые UI (Button, FormField, Modal, Toast)
│   │   │   ├── layout/                  # Layout (Header, Footer, LanguageSwitcher)
│   │   │   ├── events/                  # Event компоненты
│   │   │   ├── admin/                   # Админ компоненты
│   │   │   └── security/                # Turnstile.svelte
│   │   ├── server/                      # Server-side код
│   │   │   ├── db/                      # Database операции
│   │   │   │   ├── index.ts             # DB helper (getDB)
│   │   │   │   ├── users.ts             # CRUD пользователей
│   │   │   │   ├── events.ts            # CRUD мероприятий
│   │   │   │   ├── registrations.ts     # CRUD записей
│   │   │   │   ├── admin.ts             # Операции с админами
│   │   │   │   ├── activityLog.ts       # Логирование действий
│   │   │   │   └── eventFields.ts       # Дополнительные поля событий
│   │   │   ├── auth/
│   │   │   │   └── index.ts             # Auth утилиты (JWT, bcrypt, cookies)
│   │   │   ├── email/
│   │   │   │   ├── index.ts             # Email отправка (MailChannels)
│   │   │   │   └── templates.ts         # Email шаблоны (welcome, confirmation, etc.)
│   │   │   ├── storage/
│   │   │   │   ├── r2.ts                # Работа с R2
│   │   │   │   └── qr.ts                # Генерация QR-кодов
│   │   │   ├── middleware/
│   │   │   │   ├── auth.ts              # Middleware аутентификации
│   │   │   │   ├── admin.ts             # Middleware для админов
│   │   │   │   ├── csrf.ts              # CSRF защита
│   │   │   │   ├── turnstile.ts         # Turnstile верификация
│   │   │   │   └── errorHandler.ts      # Обработка ошибок
│   │   │   └── validation/
│   │   │       └── schemas.ts           # Zod схемы валидации
│   │   ├── types/                       # TypeScript типы
│   │   │   ├── index.ts
│   │   │   ├── user.ts
│   │   │   ├── event.ts
│   │   │   └── registration.ts
│   │   ├── stores/                      # Svelte stores
│   │   │   ├── user.ts                  # Store текущего пользователя
│   │   │   └── language.ts              # Store языка (i18n)
│   │   └── validation/
│   │       └── schemas.ts               # Client-side Zod схемы
│   │
│   ├── app.d.ts                         # TypeScript декларации (App.Platform, App.PageData)
│   ├── hooks.server.ts                  # SvelteKit server hooks
│   └── worker.ts                        # Cloudflare Cron handler
│
├── static/
│   └── translations/                    # i18n переводы
│       ├── de.json
│       ├── en.json
│       ├── ru.json
│       └── uk.json
│
├── migrations/                          # D1 миграции
│   ├── 0001_initial.sql
│   └── 0002_make_max_participants_nullable.sql
│
├── docs/                                # 📚 Документация
│   ├── README.md
│   ├── development/
│   │   ├── DEPLOYMENT.md                # ⭐ Главная инструкция по деплою
│   │   ├── PROGRESS.md
│   │   └── fixes/                       # История исправлений
│   ├── database/                        # Документация БД
│   │   ├── README.md
│   │   ├── activitylog/
│   │   ├── admin/
│   │   ├── events/
│   │   ├── registrations/
│   │   ├── users/
│   │   └── gdpr/
│   └── features/                        # Функционал
│       ├── email/
│       ├── i18n/
│       ├── validation/
│       └── storage/
│
├── wrangler.toml                        # Cloudflare Workers config
├── package.json
├── svelte.config.js
├── vite.config.ts
└── tsconfig.json
```

---

## 🗄️ СТРУКТУРА БАЗЫ ДАННЫХ (D1)

### Основные таблицы:

1. **users** - Пользователи
   - id, email, password_hash
   - first_name, last_name, birth_date
   - address (street, number, zip, city)
   - contacts (phone, whatsapp, telegram)
   - consents (photo_video_consent, parental_consent, gdpr_consent)
   - preferred_language, is_blocked
   - created_at, updated_at

2. **events** - Мероприятия
   - id
   - title_de/en/ru/uk (мультиязычные поля)
   - description_de/en/ru/uk
   - requirements_de/en/ru/uk
   - location_de/en/ru/uk
   - date, registration_deadline
   - max_participants (nullable)
   - telegram_link, whatsapp_link
   - qr_telegram_url, qr_whatsapp_url (R2 URLs)
   - status (draft/active/cancelled)
   - created_at, updated_at, created_by

3. **event_additional_fields** - Дополнительные поля для событий
   - id, event_id, field_key, field_type
   - required, field_options (JSON)
   - label_de/en/ru/uk
   - placeholder_de/en/ru/uk

4. **registrations** - Записи на мероприятия
   - id, user_id, event_id
   - additional_data (JSON)
   - registered_at, cancelled_at, cancellation_reason

5. **admins** - Администраторы
   - id, user_id, created_by, created_at
   - created_by = NULL → superadmin (неудаляемый)

6. **activity_log** - Лог действий
   - id, user_id, action_type, details
   - ip_address, timestamp

7. **deleted_users_archive** - Архив удалённых пользователей (GDPR)
   - id, first_name, last_name
   - registered_at, deleted_at
   - events_participated (JSON)

8. **pending_deletions** - Запланированные удаления (GDPR)
   - id, user_id, deletion_date, created_at

---

## 🔧 КЛЮЧЕВЫЕ КОМПОНЕНТЫ

### 1. Аутентификация (`src/lib/server/auth/index.ts`)

- `hashPassword()` - bcryptjs хеширование
- `verifyPassword()` - проверка пароля
- `generateToken()` - JWT через jose (Uint8Array secret)
- `verifyToken()` - проверка JWT
- `setAuthCookie()` - установка httpOnly cookie
- `clearAuthCookie()` - удаление cookie
- `extractTokenFromRequest()` - извлечение токена из cookies

### 2. Email (`src/lib/server/email/index.ts`)

- `sendEmail()` - отправка через MailChannels HTTP API
- `sendBulkEmails()` - массовая отправка с батчингом
- `sendEmailSafely()` - обёртка для безопасной отправки (не блокирует основной процесс)
- DKIM форматирование (удаление заголовков BEGIN/END, PEM формат 64 символа/строка)

### 3. i18n (`src/lib/stores/language.ts`)

- `initializeI18n()` - инициализация svelte-i18n (SSR safe)
- `changeLanguage()` - переключение языка
- `currentLanguage` - store текущего языка
- Fallback: de → en → ru → uk

### 4. Turnstile (`src/lib/components/security/Turnstile.svelte`)

- Автозагрузка SDK
- `reset()` метод для сброса виджета после ошибок
- Интеграция на `/register`, `/login`, `/admin/newsletter`

### 5. GDPR Compliance

- Автоматическое удаление через 28 дней после последнего мероприятия
- Cron триггер (каждый день в 02:00 UTC)
- Архивирование минимальных данных (имя, даты, список событий)
- Блокировка доступа при запланированном удалении (`is_blocked = 1`)

---

## 🔐 БЕЗОПАСНОСТЬ

- **JWT** в httpOnly cookies (7 дней)
- **CSRF** защита (double-submit cookie)
- **Cloudflare Turnstile** (защита от ботов)
- **bcrypt** для паролей (10 rounds)
- **Prepared statements** в SQL (защита от injection)
- **Zod валидация** на клиенте и сервере
- **DKIM подпись** для email (MailChannels)

---

## 🌍 МУЛЬТИЯЗЫЧНОСТЬ

- **Языки:** de (дефолтный), en, ru, uk
- **Server-side:** определение языка из cookie → Accept-Language → дефолт
- **Client-side:** localStorage + browser language
- **Fallback:** если поле события пустое для текущего языка → показывается немецкий вариант
- **Email:** шаблоны на всех языках

---

## 📧 EMAIL СИСТЕМА

### Провайдер: MailChannels

**Типы писем:**

1. Welcome email (после регистрации)
2. Event registration confirmation (после записи)
3. Event cancellation by user (после отмены записи)
4. Event cancelled by admin (отмена мероприятия админом)
5. Bulk newsletter (массовая рассылка)

**Батчинг:**

- Массовая рассылка идёт порциями (EMAIL_BULK_CHUNK = 50)
- Пауза между порциями (EMAIL_BULK_PAUSE_MS = 60000 = 1 минута)

**DKIM:**

- Domain: kolibri-dresden.de
- Selector: mailchannels
- Private key: в Cloudflare Secrets
- Public key: в DNS (TXT запись `mailchannels._domainkey`)

---

## 🚀 CLOUDFLARE WORKERS КОНФИГУРАЦИЯ

### Bindings:

- `env.DB` - D1 Database
- `env.R2_BUCKET` - R2 Storage
- `env.ASSETS` - Static assets

### Environment Variables:

- `EMAIL_PROVIDER` = "mailchannels"
- `EMAIL_FROM` = "Berufsorientierung <Berufsorientierung@kolibri-dresden.de>"
- `EMAIL_REPLY_TO` = "Berufsorientierung <Berufsorientierung@kolibri-dresden.de>"
- `EMAIL_BULK_CHUNK` = "50"
- `EMAIL_BULK_PAUSE_MS` = "60000"
- `TURNSTILE_SITE_KEY` = "0x4AAAAAACAvvssouRLKagg3"

### Secrets:

- `JWT_SECRET` - секрет для JWT (64+ символов)
- `DKIM_DOMAIN` - kolibri-dresden.de
- `DKIM_SELECTOR` - mailchannels
- `DKIM_PRIVATE_KEY` - RSA приватный ключ (PEM формат)
- `R2_PUBLIC_URL` - https://pub-xxxxx.r2.dev
- `CRON_SECRET` - секрет для HTTP cron endpoint
- `SETUP_TOKEN` - токен для тестового email endpoint
- `TURNSTILE_SECRET_KEY` - секрет для Turnstile верификации

### Cron Triggers:

- `0 2 * * *` - автоматическое удаление пользователей (каждый день в 02:00 UTC)

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ (ПОСЛЕ ДЕПЛОЯ)

### 1. Email отправка

**Статус:** ⚠️ Частично исправлено  
**Проблема:** MailChannels 401 Unauthorized  
**Причина:** MailChannels через Cloudflare разрешает отправку только “от доменов, принадлежащих этому Cloudflare‑аккаунту”. SPF/DKIM/DMARC на IONOS важны для доставляемости, но не дают Worker право слать “от kolibri-dresden.de”. Отсюда скорее всего и получаем 401.  
**Решение:** Пересозданы DKIM ключи, обновлена DNS запись, обновлён secret  
**Требует тестирования:** Да

### 2. Turnstile timeout-or-duplicate

**Статус:** ⚠️ Частично исправлено  
**Проблема:** Токен истекает, пользователь не может повторно отправить форму  
**Решение:** Добавлен метод `reset()` в Turnstile компонент, автоматический сброс после ошибок  
**Требует тестирования:** Да

### 3. i18n SSR ошибки

**Статус:** ✅ Исправлено  
**Проблема:** `Cannot format a message without first setting the initial locale`  
**Решение:** Добавлена серверная инициализация языка в `+layout.server.ts`, SSR-safe `initializeI18n()`  
**Требует тестирования:** Нет

### 4. JWT Token Generation

**Статус:** ✅ Исправлено  
**Проблема:** jose ожидает Uint8Array, а не CryptoKey  
**Решение:** Убран `crypto.subtle.importKey()`, используется `new TextEncoder().encode(secret)`  
**Требует тестирования:** Нет

### 5. Другие потенциальные проблемы

- **UI/UX**: возможны проблемы с интерфейсом (не отображаются кнопки, искажения в отображении панели инструментов), мобильной адаптацией
- **Бизнес-логика**: возможны баги в записи на мероприятия, отмене, проверке дедлайнов
- **Валидация**: возможны несоответствия между клиентской и серверной валидацией
- **Производительность**: возможны проблемы с загрузкой при большом количестве мероприятий
- **Accessibility**: возможны проблемы с доступностью (ARIA, keyboard navigation)

---

## 📋 ИНСТРУМЕНТЫ ДЛЯ ОТЛАДКИ

### Логи в реальном времени:

```bash
wrangler tail
wrangler tail | findstr /i error
wrangler tail | findstr /i email
```

### Проверка DNS:

```bash
nslookup -type=TXT kolibri-dresden.de
nslookup -type=TXT mailchannels._domainkey.kolibri-dresden.de
nslookup -type=TXT _dmarc.kolibri-dresden.de
```

### Проверка БД:

```bash
wrangler d1 execute berufsorientierung-db --remote --command="SELECT * FROM users LIMIT 5;"
wrangler d1 execute berufsorientierung-db --remote --command="SELECT * FROM admins;"
wrangler d1 execute berufsorientierung-db --remote --command="SELECT * FROM events;"
```

### Проверка Secrets:

```bash
wrangler secret list
```

---

## 🎯 ТВОЯ РОЛЬ

Ты - **опытный fullstack разработчик**, работающий над **post-deployment debugging** проекта Berufsorientierung.

**Проект задеплоен, но требует доработки.**

### Твои задачи:

1. **Анализировать проблемы**, которые будут описаны в следующих промптах
2. **Диагностировать причины** багов/проблем
3. **Предлагать решения** с объяснением
4. **Вносить исправления** в код
5. **Тестировать** исправления (где возможно)
6. **Документировать** важные изменения (только по запросу)

### Принципы работы:

- **Минималистичные изменения** - исправляй только то, что сломано
- **Не ломай работающее** - проверяй зависимости перед изменениями
- **Тестируй после изменений** - используй `get_errors`, проверяй логику
- **Комментарии на русском** - все комментарии в коде на русском языке
- **Не создавай MD файлы** без явного запроса

### Контекст для работы:

- **Доступ к коду:** полный (через file operations)
- **Доступ к логам:** через `wrangler tail`
- **Доступ к БД:** через `wrangler d1 execute`
- **Документация:** в `docs/` (при необходимости читай для контекста)

---

## ⏸️ ОЖИДАНИЕ ЗАДАНИЯ

**Я готов к работе!**

Опиши конкретную проблему, которую нужно исправить, и я:

1. Проанализирую её причины
2. Найду нужные файлы в кодовой базе
3. Предложу решение
4. Внесу исправления
5. Проверю результат

**Формат задания:**

```
Проблема: [краткое описание]
Симптомы: [что происходит/не происходит]
Логи: [вывод wrangler tail, если есть]
Ожидаемое поведение: [как должно работать]
```

**Жду следующего промпта с описанием проблемы для исправления!** 🚀
