# SvelteKit invalidateAll Fix

**Дата:** 11 ноября 2025  
**Категория:** SvelteKit Navigation  
**Статус:** ✅ Исправлено

---

## 🐛 Проблема

В `src/routes/admin/registrations/+page.svelte` использовался **несуществующий параметр** функции `goto()`:

```typescript
// ❌ НЕПРАВИЛЬНО: invalidateAll не является параметром goto()
goto('/admin/registrations', { invalidateAll: true });
```

**Причина ошибки:**

В SvelteKit функция `goto()` имеет следующую сигнатуру:

```typescript
function goto(
	url: string | URL,
	opts?: {
		replaceState?: boolean;
		noScroll?: boolean;
		keepFocus?: boolean;
		invalidate?: boolean; // ← не invalidateAll!
		state?: any;
	}
): Promise<void>;
```

Параметра `invalidateAll` в опциях **не существует**. Для инвалидации всех данных нужно использовать **отдельную функцию** `invalidateAll()`.

---

## ✅ Решение

### 1. Импортировать функцию `invalidateAll`

```typescript
// Было:
import { goto } from '$app/navigation';

// Стало:
import { goto, invalidateAll } from '$app/navigation';
```

### 2. Вызвать `invalidateAll()` перед навигацией

```typescript
// Было:
if (response.ok) {
	alert($_('admin.registrations.cancelled_success'));
	showModal = false;
	selectedRegistration = null;
	cancellationReason = '';
	// Перезагрузка страницы
	goto('/admin/registrations', { invalidateAll: true }); // ❌
}

// Стало:
if (response.ok) {
	alert($_('admin.registrations.cancelled_success'));
	showModal = false;
	selectedRegistration = null;
	cancellationReason = '';
	// Инвалидация данных и перезагрузка страницы
	await invalidateAll(); // ✓
	goto('/admin/registrations'); // ✓
}
```

---

## 📚 Справка по SvelteKit Navigation

### `goto(url, opts?)`

Программная навигация на другую страницу.

**Параметры:**

- `url` - URL для перехода
- `opts.replaceState` - заменить текущую запись в истории (вместо добавления новой)
- `opts.noScroll` - не скроллить к началу страницы
- `opts.keepFocus` - сохранить текущий фокус
- `opts.invalidate` - **устарело**, используйте `invalidateAll()`
- `opts.state` - состояние для `$page.state`

### `invalidateAll()`

Инвалидирует все `load` функции на текущей странице, вызывая их повторный запуск.

**Когда использовать:**

- После мутации данных (CREATE, UPDATE, DELETE)
- Когда нужно перезагрузить все данные страницы
- После административных действий (отмена регистрации, блокировка пользователя и т.д.)

**Пример:**

```typescript
// После успешной мутации данных
await fetch('/api/admin/registrations/123/cancel', {
	method: 'POST',
	body: JSON.stringify({ reason: 'Admin action' }),
});

// Инвалидируем все данные страницы
await invalidateAll();

// Опционально: переход на другую страницу
goto('/admin/registrations');
```

### `invalidate(resource)`

Инвалидирует конкретные `load` функции, которые зависят от указанного ресурса.

**Пример:**

```typescript
// Инвалидировать только загрузку конкретного ресурса
await invalidate('/api/registrations');
```

---

## 📦 Изменённые файлы

1. **`src/routes/admin/registrations/+page.svelte`**
   - Добавлен импорт `invalidateAll`
   - Заменён `goto(..., { invalidateAll: true })` на `await invalidateAll(); goto(...)`

2. **`docs/development/fixes/SVELTEKIT_INVALIDATION.md`** (новый)
   - Документация по исправлению

---

## 🎯 Результат

### Преимущества:

✅ **Правильное использование API** - соответствует документации SvelteKit  
✅ **Предсказуемое поведение** - данные гарантированно перезагружаются  
✅ **Нет ошибок** - не используются несуществующие параметры  
✅ **Async/await** - явное управление асинхронными операциями

### Поведение после исправления:

1. Пользователь отменяет регистрацию через админ-панель
2. Отправляется POST запрос на `/api/admin/registrations/[id]/cancel`
3. При успехе:
   - Показывается alert с подтверждением
   - Закрывается модальное окно
   - Вызывается `invalidateAll()` → все `load` функции перезапускаются
   - Выполняется `goto('/admin/registrations')` → переход на список регистраций
4. Страница отображает актуальные данные (без отменённой регистрации)

---

## 🔗 Связанные документы

- [SvelteKit Navigation Docs](https://kit.svelte.dev/docs/modules#$app-navigation)
- [Load Function Docs](https://kit.svelte.dev/docs/load)
- [Invalidation Docs](https://kit.svelte.dev/docs/load#rerunning-load-functions)

---

## 💡 Рекомендации

### Когда использовать `invalidateAll()`:

✅ После любой мутации данных на сервере  
✅ После административных действий  
✅ Когда нужно обновить все данные страницы

### Когда использовать `invalidate(resource)`:

✅ Когда нужно обновить только конкретный ресурс  
✅ Для оптимизации (меньше запросов)  
✅ Когда известны точные зависимости

### Когда использовать `goto()` с `replaceState`:

✅ После успешной отправки формы (чтобы F5 не отправлял повторно)  
✅ При перенаправлениях после авторизации  
✅ Когда не нужно добавлять запись в историю браузера

---

## ✅ Проверка

После исправления убедитесь, что:

1. ✓ Отмена регистрации работает корректно
2. ✓ Список регистраций обновляется автоматически
3. ✓ Нет ошибок в консоли браузера
4. ✓ Навигация "Назад/Вперёд" работает правильно
