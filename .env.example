# JWT секрет для подписи токенов (генерируйте случайную строку длиной минимум 32 символа)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email провайдер (mailchannels, resend, sendgrid, postmark)
EMAIL_PROVIDER=mailchannels

# Email адреса
EMAIL_FROM=Berufsorientierung <Berufsorientierung@kolibri-dresden.de>
EMAIL_REPLY_TO=Berufsorientierung <Berufsorientierung@kolibri-dresden.de>

# Настройки массовой рассылки (батчинг для предотвращения превышения лимитов API)
# EMAIL_BULK_CHUNK - количество писем в одном батче (рекомендуется 50)
# EMAIL_BULK_PAUSE_MS - пауза между батчами в миллисекундах (рекомендуется 60000 = 1 минута)
# Консервативные значения для максимальной надёжности и избежания blacklist
EMAIL_BULK_CHUNK=50
EMAIL_BULK_PAUSE_MS=60000

# DKIM настройки для email аутентификации (ОБЯЗАТЕЛЬНО для production!)
# DKIM подписывает письма криптографически, чтобы они не попадали в спам
# 
# 📚 Инструкция по генерации ключей и настройке DNS:
#    docs/features/email/DEPLOYMENT.md
#
# Генерация ключей:
#   openssl genrsa -out dkim_private.key 2048
#   openssl rsa -in dkim_private.key -pubout -outform der 2>/dev/null | openssl base64 -A > dkim_public.txt
#
# Приватный ключ должен быть в формате PEM с экранированными переносами строк (\n)
# Публичный ключ из dkim_public.txt размещается в DNS как TXT запись:
#   mailchannels._domainkey.kolibri-dresden.de IN TXT "v=DKIM1; k=rsa; p=<PUBLIC_KEY>"
#
DKIM_DOMAIN=kolibri-dresden.de
DKIM_SELECTOR=mailchannels
DKIM_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvgIBADANBgkqhkiG9w0BAQ...\n-----END PRIVATE KEY-----"

# Cloudflare R2 публичный URL для QR-кодов
R2_PUBLIC_URL=https://your-r2-bucket.r2.dev

# ID базы данных D1 (получите после создания БД в Cloudflare)
DATABASE_ID=

# Токен для первоначальной настройки (создание первого админа)
SETUP_TOKEN=your-setup-token-for-initial-admin-creation

# Секрет для Cron триггеров (защита от несанкционированных вызовов)
CRON_SECRET=your-cron-secret-for-scheduled-tasks
